# Supabase — ShowHQ shares the StagePilot project (modular monolith)
# The app works without these (hardcoded fallbacks exist), but set them
# explicitly for production or if you spin up a separate Supabase project.
VITE_SUPABASE_URL=https://uysvtgbnvfndpijdejdx.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here
